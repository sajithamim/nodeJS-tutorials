// Create a file usig async/await
const fs = require("fs/promises");

const createFile = async () => {
  try {
    await fs.writeFile("login.html", "Welcome to your login page");
  } catch {
    console.log(err);
  }
};

createFile();
